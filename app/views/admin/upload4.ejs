<% include header %>
<div class="container">
    <div class="well">
        <div class="btn btn-success">
            <input type="file" id="file" onchange="onselectimage()" style="filter:alpha(opacity=0);opacity:0;width:100%;height:100%;"/>
        </div>
    </div>
</div>

<%- include resource %>

<script>
    //选择文件之后直接上传
    function onselectimage() {
        var filenode = document.getElementById('file');
        //构造form数据
        var form = new FormData();
        form.append("file",filenode.files[0]);
        $.ajax({
            url:'/admin/upload4',
            type:'post',
            data:postData,
            dataType:'json',
            success:function(data){
                console.log(data);
            },
            error:function(err){
                console.log(err);
            }
        });
    }
da
</script>
<% include footer %>